# Claude Code config: .claude/settings.json, hooks/ and commands/ are COMMITTED
# (shared project setup — see .claude/README.md). Only personal overrides are local.
.claude/settings.local.json

# OS
Thumbs.db
.DS_Store

# sounds/ is COMMITTED (~19 MB of OGG/WAV). Keep new audio compressed —
# tests/test_media_budget.py fails on any file over 4 MB or any WAV ambient track.

# Python build cache
__pycache__/
*.pyc

# The Workshop build: scripts/publish.py rebuilds it for each release tag,
# and the copy that matters is the one uploaded to the Workshop.
saves/StarveNoMore.publish.json

# Single-card preview extracts produced by ad-hoc cropping
art/decks/_test_*.png
